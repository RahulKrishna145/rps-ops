apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rps-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rps-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rps-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
